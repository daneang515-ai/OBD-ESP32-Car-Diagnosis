# 🔧 故障排查 (TROUBLESHOOTING)

> 从底层到上层，逐层排查问题

---

## 📑 排查思路

**核心原则：自下而上，逐层验证**

```
Layer 5: 鸿蒙 App UI        → 看数据是否显示
Layer 4: 蓝牙通信           → 看串口是否有数据
Layer 3: STM32 程序         → 看 LED/串口输出
Layer 2: CAN 总线           → 看 CAN 收发是否正常
Layer 1: 硬件接线           → 看电源/地线/引脚
```

**快速定位：**

```bash
# 用 PC + USB-TTL 连接 HC-05 的串口（或 STM32 的 USART2）
# 打开串口助手，波特率 9600
# 手动发送: 010C\n
# 观察是否有 CAN 数据返回
```

---

## 一、硬件问题

### 1.1 STM32 不上电 / 不运行

| 检查项 | 方法 |
|--------|------|
| 5V 供电 | 万用表测 STM32 5V 引脚是否有 5V |
| 3.3V | 测稳压芯片输出是否有 3.3V |
| 晶振 | 示波器看 HSE 是否有 8MHz 波形 |
| BOOT0 | 确认 BOOT0 接 GND（从 Flash 启动） |
| 复位 | 按复位键，看 LED 是否闪烁 |

### 1.2 TJA1050 不工作

| 检查项 | 方法 |
|--------|------|
| VCC | 测 TJA1050 VCC = 5V |
| GND | 测 GND 是否连通（**关键！**） |
| CAN_H/L | 空闲时 CAN_H ≈ 2.5V, CAN_L ≈ 2.5V |
| 终端电阻 | 测 OBD Pin 6-Pin 14 之间是否有 ~120Ω（车辆内部） |
| 120Ω 电阻 | 若车辆无终端电阻，TJA1050 模块需自带 |

### 1.3 HC-05 不工作

| 检查项 | 方法 |
|--------|------|
| VCC | 测 HC-05 VCC = 5V（部分需 3.3V，看模块说明） |
| LED 状态 | 快闪 = 未配对，慢闪 = 已连接 |
| 波特率 | **必须 9600**，可用 AT 指令查询 |
| 接线 | TX→RX, RX→TX（交叉！） |

---

## 二、CAN 总线问题

### 2.1 CAN 完全无通信

**排查顺序：**

1. **TJA1050 GND 是否与 OBD Pin4/5 连通**
   ```bash
   # 万用表二极管档
   # 测 TJA1050 GND ↔ OBD Pin 4/5 是否导通
   ```

2. **CAN_H / CAN_L 是否接反**
   - 交换试试（CAN 差分信号，反了不工作）

3. **波特率是否匹配**
   - 默认 500Kbps
   - 老车/商用车可能是 250Kbps
   - App 发送 `BAUD:250` 切换试试

4. **车辆是否支持 CAN-OBD**
   - 2008 年后汽油车基本都支持
   - 更早的车可能用 ISO9141 (K-Line) 或 J1850

5. **ST-Link 是否拔掉了**
   - ⚠️ **上车必须拔掉**，否则干扰 CAN 总线

### 2.2 CAN 有数据但乱码

| 原因 | 解决 |
|------|------|
| 波特率不匹配 | 切换 250K/500K |
| 终端电阻缺失 | 加 120Ω 终端电阻 |
| 线缆过长/干扰 | 缩短 CAN 线，远离高压线 |
| TJA1050 供电不稳 | 加 100nF 去耦电容 |

### 2.3 CAN 总线 BUS-OFF

**现象**：STM32 CAN 状态变为 BUS-OFF，停止收发

**原因**：
- 连续发送错误帧超过阈值
- 接线短路（CAN_H 对地/电源短路）

**解决**：
```c
// 在 HAL_CAN_ErrorCallback 中恢复
void HAL_CAN_ErrorCallback(CAN_HandleTypeDef *hcan) {
    if (hcan->ErrorCode & HAL_CAN_ERROR_BOF) {
        // 总线关闭，重新初始化
        HAL_CAN_Stop(hcan);
        HAL_CAN_Init(hcan);
        HAL_CAN_Start(hcan);
        HAL_CAN_ActivateNotification(hcan, CAN_IT_RX_FIFO0_MSG_PENDING);
    }
}
```

---

## 三、蓝牙通信问题

### 3.1 蓝牙连不上

| 原因 | 解决 |
|------|------|
| HC-05 未进入可发现模式 | 通电后 LED 快闪 = 可发现 |
| 已配对其他设备 | 长按 HC-05 按键进入 AT 模式，清除配对 |
| 密码错误 | 默认 `1234`，部分 `0000` |
| 距离太远 | 保持 < 10 米，无遮挡 |
| 鸿蒙蓝牙权限未授予 | 设置 → 应用 → OBD App → 开启蓝牙权限 |

### 3.2 连上了但没数据

**排查步骤：**

1. **确认 HC-05 波特率 9600**
   ```bash
   # 进入 AT 模式（按住按键通电）
   # 串口发送: AT+UART?
   # 应返回: +UART:9600,0,0
   # 若不是: AT+UART=9600,0,0
   ```

2. **确认 STM32 USART2 波特率 9600**
   - CubeIDE 检查 USART2 配置
   - 或用示波器看 PA2 波形

3. **串口回环测试**
   - PC 串口助手连接 HC-05
   - 发送 `010C\n`
   - 用逻辑分析仪/示波器看 STM32 是否收到
   - 或 STM32 收到后原样回传，看 PC 是否收到

4. **TX/RX 交叉**
   - STM32 PA2(TX) → HC-05 RX ✅
   - STM32 PA3(RX) → HC-05 TX ✅
   - ⚠️ 别接反了！

### 3.3 数据丢包 / 延迟

| 原因 | 解决 |
|------|------|
| 波特率太低 | 可提高 HC-05 到 38400/115200（需同步改 STM32） |
| 蓝牙干扰 | 避开 WiFi 信道，缩短距离 |
| App 请求过快 | 控制请求间隔 ≥ 100ms |

---

## 四、鸿蒙 App 问题

### 4.1 模拟器无法调试蓝牙

**这是已知限制，鸿蒙模拟器无蓝牙硬件。**

**解决**：必须使用**真机平板**调试。

### 4.2 App 闪退

| 原因 | 解决 |
|------|------|
| 蓝牙权限未授予 | 设置 → 应用 → OBD → 开启所有权限 |
| SPP UUID 错误 | 确认 `00001101-0000-1000-8000-00805F9B34FB` |
| API 版本不兼容 | 确认 DevEco Studio ≥ 4.0，SDK ≥ API 9 |

### 4.3 连接成功但读不到数据

1. **确认蓝牙 SPP 已连接**（系统蓝牙设置里显示已连接）
2. **确认 App 选择了正确的设备**
3. **用 `can_sniffer.py` 验证硬件链路**
   ```bash
   python tools/can_sniffer.py COM3
   # 如果这里能看到数据 → 硬件OK，问题在App
   # 如果看不到 → 问题在 STM32/蓝牙链路
   ```

### 4.4 UI 不更新

- 确认 `@State` 变量正确绑定
- 确认 `onResponse` 回调已注册
- 确认数据解析逻辑正确

---

## 五、供电问题

### 5.1 STM32 上车后不断重启

**原因**：供电不足

**解决**：
- 检查 12V→5V 模块输出电流 ≥ 500mA
- 建议用 MP1584（效率更高）或 LM2596
- OBD Pin 16 直接取电可能电流不足，建议独立取电

### 5.2 电压不稳导致 CAN 异常

**解决**：在 STM32 VCC 和 GND 之间加：
- 100nF 陶瓷电容（高频滤波）
- 10µF 电解电容（低频滤波）

---

## 六、常见问题速查表

| 现象 | 最可能原因 | 快速解决 |
|------|-----------|----------|
| CAN 无通信 | TJA1050 未共地 | 接好 OBD GND |
| CAN 无通信 | CAN_H/L 接反 | 交换接线 |
| CAN 无通信 | 波特率不对 | 切换 250K/500K |
| 蓝牙连不上 | 未配对 | 系统设置配对 1234 |
| 蓝牙无数据 | 波特率不匹配 | 确认两端 9600 |
| 上车重启 | 供电不足 | 换大功率降压模块 |
| 乱码 | 波特率/接地 | 检查波特率+共地 |
| App 无数据 | 模拟器无蓝牙 | 换真机 |
| BUS-OFF | 接线短路 | 检查 CAN_H/L |

---

## 七、调试工具推荐

| 工具 | 用途 |
|------|------|
| 万用表 | 测电压、通断 |
| 示波器 | 看 CAN 波形、波特率 |
| 逻辑分析仪 | 抓 UART/CAN 时序 |
| USB-TTL 串口 | PC 连接 HC-05 调试 |
| `can_sniffer.py` | 嗅探 CAN 数据 |
| STM32CubeMonitor | 实时变量监控 |

---

## 八、获取帮助

如果以上方法都无法解决：

1. **开启调试日志**
   ```c
   // STM32 端增加详细日志
   printf("[CAN] TX: ID=%03lX Data=%02X...\n", id, data[0]);
   printf("[UART] RX: %s\n", uart_rx_buffer);
   ```

2. **收集信息**
   - 车辆品牌/型号/年份
   - STM32 串口日志
   - CAN 抓包数据
   - App 报错截图

3. **提交 Issue**
   - GitHub Issues: https://github.com/daneang515-ai/OBD-ESP32-Car-Diagnosis/issues
   - 附上上述信息

---

## 💡 预防建议

- ✅ 先用面包板回环测试 CAN，再接车
- ✅ 上车前**务必拔掉 ST-Link**
- ✅ **务必共地**（OBD GND 接好）
- ✅ 波特率严格 9600（除非刻意修改）
- ✅ 加电源滤波电容
- ✅ 用真机调试鸿蒙 App

---

**祝你调试顺利！🛠️**
